#ifndef MATH_FUNCTIONS_H_
#define MATH_FUNCTIONS_H_

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int is_multiple(char* x);
int is_prime(int x);
int sum(char* x);
long factorial(long x);

#endif